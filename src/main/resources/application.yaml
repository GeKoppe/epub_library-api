server:
  port: 9093
  context-path: /api/v1
  tomcat:
    max-swallow-size: 50MB


spring:
  config:
    import: optional:.env[.properties]
  servlet:
    context-path: /api/v1
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${DB_NAME:epub-lib}
    username: ${POSTGRES_USER:epub}
    password: ${POSTGRES_PW:epub}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true